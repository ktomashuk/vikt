import React from 'react';
import Container from '@material-ui/core/Container';

const Placeholder = props => {

    return(
        <div>
            <Container >
                Этой страницы пока не существует :(
            </Container>
        </div>
    );
};

export default Placeholder;